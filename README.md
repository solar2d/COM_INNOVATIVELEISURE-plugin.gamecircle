Sources for the plugin `plugin.gamecircle`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.gamecircle" = {
            publisherId = "COM_INNOVATIVELEISURE",
        },
    },
}
```
